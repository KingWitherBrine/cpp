#include <iostream>
#include <vector>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    int c, d;
    cin >> c >> d;
    int temp_matrix [] = min((a, b), (c, d));
    int count;
    0;
    if (temp_matrix == (a, b)){

    }

}